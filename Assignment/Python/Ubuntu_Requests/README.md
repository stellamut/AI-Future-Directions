# Ubuntu_Requests
# 🌍 Ubuntu Image Fetcher

**"I am because we are."**  
This project embodies the spirit of Ubuntu—community, respect, and sharing—by connecting to the global web, fetching shared images, and organizing them for future appreciation.

## 📌 Features
- Prompts user for an image URL
- Downloads the image using `requests`
- Saves it to a local folder called `Fetched_Images`
- Handles errors gracefully and respectfully
- Generates a fallback filename if none is provided

## 🛠 Requirements
- Python 3.x
- `requests` library

Install dependencies:
```bash
pip install requests
